
using System.Text.RegularExpressions;
namespace Tag.Vows
{
    class StaticPage : BasePage
    {
        public StaticPage(string mHtmlpPath, string mPageName, int mDeep, mPaths path)
            : base(mHtmlpPath, mPageName, mDeep, path)
        {
        }
    }
}